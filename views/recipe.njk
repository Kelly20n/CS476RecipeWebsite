{% include './includes/head.njk' %}

<div class='recipe'>
    <div>{{post.title}}</div><br><Br>
    <div>Ingredients:<br><Br>{{post.ingredients}}</div><br><Br>
    <div>Instructions:<br><Br>{{post.instructions}}</div><br><Br>
</div>

<div class='comment'>
    <div>Comments</div>
    
    {# <form action="/view/{{post.id}}" onsubmit="setTimeout(function(){window.location.reload();},100);" method="POST">
        <div>
            <textarea placeholder="Add a Comment" name="userComment"></textarea>
            <input type="submit"/>
        </div>
    </form> #}

    <form action="/view/{{post.id}}" method="POST">
        <div>
            <textarea placeholder="Add a Comment" name="userComment"></textarea>
            <input type="submit"/>
        </div>
    </form>
    
    {% for comment in comments %}
    <div class='comment'>
        <div> {{comment.commentBody}}</div><br><Br>
    </div>

    {% endfor %}
    
    
</div>

